browser=chrome  # Can be chrome or edge (chrome_headless, chrome_emulator tbd)
local=true  # true = run locally, false = run on Selenium Grid
grid.url=http://localhost:4444/wd/hub  # URL for Selenium Grid Hub